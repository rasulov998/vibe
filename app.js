const toast=document.getElementById('toast');const searchInput=document.getElementById('searchInput');function showToast(msg){toast.textContent=msg;toast.classList.add('show');clearTimeout(window.t);window.t=setTimeout(()=>toast.classList.remove('show'),1800)}document.querySelectorAll('[data-like]').forEach(btn=>btn.addEventListener('click',()=>{btn.textContent=btn.textContent==='♡'?'♥':'♡';showToast(btn.textContent==='♥'?'Лайк поставлен ❤️':'Лайк убран')}));document.querySelectorAll('.nav-item,.bottom-nav button').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.nav-item,.bottom-nav button').forEach(x=>x.classList.remove('active'));document.querySelectorAll('[data-tab="'+btn.dataset.tab+'"]').forEach(x=>x.classList.add('active'));if(btn.dataset.tab!=='home')showToast(btn.dataset.tab==='create'?'Открываем создание видео…':btn.dataset.tab==='profile'?'Открываем профиль…':'Раздел VIBE скоро будет здесь')}));document.querySelectorAll('.tab').forEach(tab=>tab.addEventListener('click',()=>{document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));tab.classList.add('active');showToast('Лента: '+tab.textContent)}));searchInput.addEventListener('keydown',e=>{if(e.key==='Enter'&&searchInput.value.trim())showToast('Поиск: '+searchInput.value.trim())});document.getElementById('searchBox').addEventListener('click',()=>searchInput.focus());document.getElementById('themeBtn').addEventListener('click',()=>showToast('VIBE 2.0 сейчас в тёмной теме'));
const authModal=document.getElementById('authModal'),authForm=document.getElementById('authForm'),authTitle=document.getElementById('authTitle'),authSubmit=document.getElementById('authSubmit'),registerField=document.querySelector('.register-only');let authMode='login';function openAuth(mode='login'){authMode=mode;authModal.classList.add('open');authModal.setAttribute('aria-hidden','false');document.querySelectorAll('.auth-tab').forEach(x=>x.classList.toggle('active',x.dataset.authTab===mode));registerField.classList.toggle('hidden',mode!=='register');authTitle.textContent=mode==='register'?'Создай аккаунт в VIBE':'Добро пожаловать в VIBE';authSubmit.textContent=mode==='register'?'Создать аккаунт':'Войти'}document.querySelector('.avatar').addEventListener('click',()=>openAuth(localStorage.getItem('vibeUser')?'login':'register'));document.querySelectorAll('[data-close-auth]').forEach(x=>x.addEventListener('click',()=>{authModal.classList.remove('open');authModal.setAttribute('aria-hidden','true')}));document.querySelectorAll('.auth-tab').forEach(x=>x.addEventListener('click',()=>openAuth(x.dataset.authTab)));authForm.addEventListener('submit',e=>{e.preventDefault();const email=document.getElementById('authEmail').value.trim().toLowerCase(),password=document.getElementById('authPassword').value,username=document.getElementById('authUsername').value.trim();if(password.length<6)return showToast('Пароль: минимум 6 символов');if(authMode==='register'){if(!username)return showToast('Введи имя пользователя');localStorage.setItem('vibeUser',JSON.stringify({username,email,password}));showToast('Аккаунт создан 🎉')}else{const saved=JSON.parse(localStorage.getItem('vibeUser')||'null');if(!saved||saved.email!==email||saved.password!==password)return showToast('Email или пароль неверный');showToast('С возвращением 👋')}authModal.classList.remove('open');authModal.setAttribute('aria-hidden','true');const u=JSON.parse(localStorage.getItem('vibeUser')||'null');if(u)document.querySelector('.avatar').textContent=(u.username||'R').slice(0,1).toUpperCase())});