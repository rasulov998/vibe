# VEYRO controlled Flutter template

This template is the safe starting point for generated V1 Android apps.

The Build Worker copies this template into an isolated workspace, then the Project Engine maps a validated ProjectManifest into controlled files.

The template deliberately contains no provider secrets and no arbitrary build commands.
